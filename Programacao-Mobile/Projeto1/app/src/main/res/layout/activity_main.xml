<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">
        <TextView
            android:id="@+id/txtTitulo"
            android:layout_width="match_parent"
            android:layout_height="107dp"
            android:gravity="center_horizontal"
            android:text="@string/app_name"
            app:autoSizeMaxTextSize="?android:attr/windowTitleSize"
            app:autoSizeMinTextSize="?attr/minTouchTargetSize"
            app:autoSizeTextType="uniform" />
        <RadioGroup
            android:id="@+id/rgForma"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center|center_vertical">
            <RadioButton
                android:id="@+id/rdCircunferencia"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="calcularCircunferencia"
                android:text="@string/circunferencia"
                android:textSize="36sp" />
            <RadioButton
                android:id="@+id/rdRetangulo"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:onClick="calcularRetangulo"
                android:text="@string/retangulo"
                android:textSize="36sp" />
        </RadioGroup>
    </LinearLayout>
</RelativeLayout>